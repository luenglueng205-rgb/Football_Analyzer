# P0 - 3. Daemon Minimum Realistic Link

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Transform `market_sentinel.py` into a real daemon that correctly writes to a queue, wakes up the agent, calls the ledger, and sends notifications.

**Architecture:** Use `market_monitor.py` queue mechanisms or just implement a proper `asyncio.Queue` or loop inside `market_sentinel.py`. Since `market_monitor.py` exists and has file-locking queue logic, we can leverage that or build a simpler, reliable queue within `market_sentinel.py`. Let's implement it inside `market_sentinel.py` directly by making it fetch real data from `get_live_odds` (we will use `scrape_beidan_sp` if beidan, or standard API if jingcai, but since we mock data fetch, we'll keep the scan mocked but make the *handling* realistic).
Wait, the P0 checklist says: "let's use a real data source if possible, but at least make the trigger -> wake up -> ledger -> notify link real." Let's update the mock to actually call `check_bankroll`, trigger the agent, which will then use tools, and the agent should finish by notifying.

**Tech Stack:** Python 3.

---

### Task 1: Realistic Sentinel Daemon

**Files:**
- Modify: `market_sentinel.py`

- [ ] **Step 1: Write minimal implementation**
We will change `market_sentinel.py` to use a file-based queue for triggered events, read from it, and process it realistically. Actually, the simplest is to keep it in-memory for the daemon but handle the output properly.

```python
# We will just rewrite market_sentinel.py to have a clear trigger -> analyze -> notify flow.
```
- [ ] **Step 2: Commit**
Run: `git commit -am "feat: implement realistic sentinel daemon loop"`
