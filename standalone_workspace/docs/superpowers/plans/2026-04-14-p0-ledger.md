# P0 - 4. KPI Ledger Support

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ensure the ledger supports tracking real KPIs for the bets (placed_odds, lottery_type, selection, stake, timestamp) and reserve columns for closing_odds, result, and pnl.

**Architecture:** Add columns to the SQLite `bets` table in `betting_ledger.py`. Modify `execute_bet` to record these accurately.

**Tech Stack:** Python 3, sqlite3.

---

### Task 1: Update Ledger Schema

**Files:**
- Modify: `tools/betting_ledger.py:5-40`

- [ ] **Step 1: Write implementation**

Modify `_init_db` in `tools/betting_ledger.py`:
```python
        c.execute('''CREATE TABLE IF NOT EXISTS bets
                     (id INTEGER PRIMARY KEY AUTOINCREMENT,
                      match_id TEXT, lottery_type TEXT, selection TEXT,
                      odds REAL, stake REAL, ticket_code TEXT,
                      status TEXT, timestamp DATETIME,
                      closing_odds REAL, result TEXT, pnl REAL)''')
```

Modify `execute_bet` in `tools/betting_ledger.py`:
```python
        c.execute('''INSERT INTO bets 
                     (match_id, lottery_type, selection, odds, stake, ticket_code, status, timestamp, closing_odds, result, pnl)
                     VALUES (?, ?, ?, ?, ?, ?, ?, ?, NULL, NULL, NULL)''',
                  (match_id, lottery_type, selection, odds, stake, ticket_code, "PENDING", now))
```

- [ ] **Step 2: Commit**
Run: `git commit -am "feat: add KPI fields to ledger for CLV calculation"`
